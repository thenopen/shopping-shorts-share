# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Node
node_modules/
.next/
out/
dist/

# Work files
packages/core/workdir/*
!packages/core/workdir/.gitkeep
# 다운로드 영상 라이브러리(원본·썸네일·메타 — 런타임 산출물)
packages/core/downloads/
*.mp4
*.mp3
*.wav
*.tmp
# 단, 성우 미리듣기 샘플은 웹에 필요하므로 커밋
!packages/web/public/voices/*.mp3

# Rust / Tauri
target/
packages/desktop/src-tauri/target/

# DB
*.sqlite
*.db

# Env
.env
.env.local

# 인증/쿠키 (민감정보 — 절대 커밋 금지)
auth/
*cookies*.txt
*_state.json

# OS
Thumbs.db
desktop.ini
.DS_Store

# IDE / 로컬 도구 설정
.vscode/
.claude/
.mcp.json

# 분석용 임시 이미지
workdir_analyze/

# 서버/개발 로그 (런타임 산출물)
*_out.log
*_err.log
_dev.log
_devout.log
_deverr.log
packages/web/_dev.log
packages/core/_server_*.log

# 디버그용 임시 스크린샷/측정 스크립트 (런타임 산출물)
_*.png
**/_*.png
_*test*.py
**/_*test*.py
_*_shot.png

# 외부 도구 클론
chrome-devtools-mcp/
# ProPainter vendored repo(303MB, 가중치 191MB) — clone+자동패치로 재현(propainter_inpaint.ensure_propainter)
packages/core/app/pipeline/ProPainter/

# 서버 debug 로그(로컬 진단용)
packages/core/_server.log
packages/core/_server.err.log

# 추출된 오버레이 에셋(ae-sources/*.zip에서 scripts/extract_overlays.py로 생성) — LFS zip이 원본
packages/core/assets/overlays/

# 저장된 편집 프로젝트(로컬)
packages/core/projects/

# runtime logs (local)
packages/core/server_run.log
packages/core/server_run.err.log

# 개인 PC 전용 런처(IP/경로 하드코딩) — 공유 제외, 각자 scripts/*.ps1 사용
run_shopping_shorts.bat
폰_접속_허용.bat

# ZCode/에이전트 세션 산물 (로컬 도구)
.zcode/
.codex/

# 폰트 파일 (총 1.7GB — GitHub 부적합, 별도 배포).
# 자막 burn-in용 ttf(545개) + 웹 미리보기용 woff2(599개).
# 복구: docs/SETUP.md 또는 packages/core/assets/fonts/README 참고해 직접 다운로드.
packages/core/assets/fonts/
packages/web/public/fonts/
packages/web/app/fonts.generated.css

# 테스트/커버리지 산출물
.coverage
.coverage.*
htmlcov/
.pytest_cache/
ae-work/
